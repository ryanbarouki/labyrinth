module.exports = { GameLoop };

function GameLoop(gameBoard) {
    if (!gameBoard) {
        return;
    }

    // checks if player is on the target tile
    // change to next target card
    // add points
}